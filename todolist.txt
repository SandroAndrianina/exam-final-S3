TODOLIST SITE WEB D'ECHANGE D'OBJETS:
------------------------------------------
ETAPE 1: (Presentation)
 * Pages importants:
        -Home.php:
        -list-city.php
        -detail-needs&donations.php
        -disaster-victims-needs.php (formulaire)
        -donation-collection.php (formulaire)
        -donation-distribution.php (formulaire)

ETAPE 2: (Business)
    Models:
        -City.php
        
    Repositories:
        -CityRepository.php

    Services:
        -CityService.php

    Controllers:
        -CityController.php

 
ETAPE 3: (Database)
 * Liste tables SQL:
     - city (id(PK), name)
     - article (id(PK), name, type, unit)
     - needs (id(PK), city_id(FK de city), article_id(FK de article), quantity_requested, creation_date)
     - gift (id(PK), article_id(FK de article), total_quantity, donation_date, description)
     - distribution (id(PK), gift_id(FK de gift), needs_id(FK de needs), attributed_quantity, affectation_date)



